
<h2> 
<%= @school.branch %>'s 
<%= @student.first_name %> 
<%= @student.last_name %> 
</h2>

Email: <%= @student.email %>
<h2> Cohort </h2>

<table>
<thead>
<tr>
  <th>First Name</th>
  <th>Last Name</th>
  <th>Email</th>
</tr>
</thead>
<tbody>
    <tr>
    <td><%= @student.first_name %></td>
    <td><%= @student.last_name %></td>
    <td><%= @student.email %></td>
    </tr>
</tbody>
</table>
<br>
<br>
<a href="/schools/index">Home</a> | 
<a href="/schools/<%= @school.id %>/students/<%= @student.id %>/edit">Edit</a>

